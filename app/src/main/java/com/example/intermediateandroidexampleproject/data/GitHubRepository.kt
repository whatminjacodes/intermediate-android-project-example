package com.example.intermediateandroidexampleproject.data

class GitHubRepository(
    private val githubApi: GitHubNetworkApi
) {
    //suspend fun getPokemon(pokemonRequest: PokemonRequest) = githubApi.getAccessToken()
}